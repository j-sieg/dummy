Hello <%%= @<%= singular_name %>.email %>,

Forgot your password? You can reset it by opening the link in your browser:
<%%= <%= plural_name %>_edit_reset_my_password_url(token: @token) %>

If this wasn't you, you can safely ignore this.